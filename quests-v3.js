(function(root){'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const status=(row,character)=>{const values=character==='all'?Object.values(row.states):[row.states[character]??-1];return values.some(v=>v===1)?'Complete':values.some(v=>v===0)?'In progress':'Not started';};
const order=row=>Number(String(row.questKey||row.name).match(/(\d+)$/)?.[1]||0);
function render(host,rawRoot={}){const m=root.QuestsPage.model(rawRoot);m.rows.sort((a,b)=>a.npc.localeCompare(b.npc)||order(a)-order(b)||a.name.localeCompare(b.name));let world=0,character='all',filter='all';const paint=()=>{const inWorld=m.rows.filter(x=>x.world===world),list=inWorld.filter(x=>filter==='all'||filter==='important'&&x.important||status(x,character)===filter),done=inWorld.filter(x=>status(x,character)==='Complete').length,groups=new Map();list.forEach(x=>{if(!groups.has(x.npc))groups.set(x.npc,[]);groups.get(x.npc).push(x);});host.innerHTML=`<div class="quest-hero"><div><p class="eyebrow">Misc · imported save</p><h2>Quests</h2><p>Quest chains are grouped and ordered by their NPC. Select a character for their personal progress.</p></div><strong>${done} / ${inWorld.length} complete</strong></div><div class="quest-controls"><label>Character <select id="questCharacter"><option value="all">All characters</option>${m.characters.map(x=>`<option value="${x.id}" ${String(x.id)===String(character)?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label><span>Click a quest for requirements and rewards.</span></div><nav class="skill-tabs quest-world-tabs">${['W1','W2','W3','W4','W5','W6','W7','Other'].map((name,id)=>`<button class="skill-tab ${id===world?'active':''}" data-quest-world="${id}">${name}</button>`).join('')}</nav><nav class="skill-tabs quest-state-tabs"><button class="skill-tab ${filter==='all'?'active':''}" data-quest-filter="all">All</button><button class="skill-tab ${filter==='important'?'active':''}" data-quest-filter="important">Progression & unlocks</button><button class="skill-tab ${filter==='In progress'?'active':''}" data-quest-filter="In progress">In progress</button><button class="skill-tab ${filter==='Not started'?'active':''}" data-quest-filter="Not started">Not started</button><button class="skill-tab ${filter==='Complete'?'active':''}" data-quest-filter="Complete">Complete</button></nav><div class="quest-chain-list">${[...groups.entries()].map(([npc,quests])=>`<section class="quest-chain"><header><strong>${esc(npc)}</strong><span>${quests.filter(x=>status(x,character)==='Complete').length}/${quests.length} complete</span></header><div class="quest-compact-grid">${quests.map(x=>{const id=list.indexOf(x),s=status(x,character);return`<button class="quest-compact quest-${s.replaceAll(' ','-').toLowerCase()}" data-quest="${id}"><span>Step ${order(x)||'?'} · ${esc(s)}${x.important?' · Progression':''}</span><strong>${esc(x.name)}</strong></button>`;}).join('')}</div></section>`).join('')||'<p class="collection-note">No quests match this tab or filter.</p>'}</div><section id="questDetail" class="exp-card upgrade-detail detail-dismissed" aria-live="polite"></section>`;const detail=host.querySelector('#questDetail');host.querySelectorAll('[data-quest]').forEach(b=>b.onclick=()=>{const x=list[Number(b.dataset.quest)],s=status(x,character);detail.innerHTML=`<button class="secondary quest-detail-close">Close</button><p class="eyebrow">${esc(x.npc)} · Step ${order(x)||'?'}</p><h3>${esc(x.name)}</h3><p><strong>${esc(s)}</strong></p><p class="exp-benefit">${esc(x.description)}</p><p><strong>Requirements</strong><br>${x.requirements.length?esc(x.requirements.join(' · ')):'Talk to the NPC / complete the prior step.'}</p><p><strong>Rewards</strong><br>${x.rewards.length?esc(x.rewards.join(' · ')):'See the quest completion screen in game.'}</p>`;detail.classList.remove('detail-dismissed');detail.querySelector('.quest-detail-close').onclick=()=>detail.classList.add('detail-dismissed');});host.querySelector('#questCharacter').onchange=e=>{character=e.target.value;paint();};host.querySelectorAll('[data-quest-world]').forEach(b=>b.onclick=()=>{world=Number(b.dataset.questWorld);paint();});host.querySelectorAll('[data-quest-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.questFilter;paint();});};paint();}
const model=root.QuestsPage.model;
root.QuestsPage={...root.QuestsPage,model,render};})(typeof window!=='undefined'?window:globalThis);
