'use strict';
const fs=require('node:fs'),path=require('node:path'),zlib=require('node:zlib');
const root=__dirname,archive=path.resolve(root,'../Idleon resources/app.asar'),client=fs.readFileSync(path.resolve(root,'../audit/N.js'),'utf8'),fd=fs.openSync(archive,'r');
try{const prefix=Buffer.alloc(16);fs.readSync(fd,prefix,0,16,0);const header=Buffer.alloc(prefix.readUInt32LE(12));fs.readSync(fd,header,0,header.length,16);let entry=JSON.parse(header.toString('utf8'));for(const part of 'distBuild/static/game/lib/default.pak'.split('/'))entry=entry.files[part];const offset=8+prefix.readUInt32LE(4)+Number(entry.offset);for(let id=0;id<10;id++){const name=`DivGod${id}`,match=client.match(new RegExp(`R0i(\\d+)R1zR2R3R4y\\d+:assets%2Fdata%2F${name}\\.pngR6i(\\d+)`));if(!match)throw Error(name+' not found');const packed=Buffer.alloc(Number(match[2]));fs.readSync(fd,packed,0,packed.length,offset+Number(match[1]));fs.writeFileSync(path.resolve(root,'assets',name+'.png'),zlib.gunzipSync(packed));}console.log('Extracted 10 Divinity god sprites.');}finally{fs.closeSync(fd);}
